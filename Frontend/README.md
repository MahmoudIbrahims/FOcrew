**Prerequisites:**  Node.js

### Create the project: Open a new terminal (do not close the python one) and run:

```Bash
npm create vite@latest frontend -- --template react-ts
```
### Enter the folder:
```Bash
cd frontend
```
### Install dependencies:
```bash
npm install lucide-react
```

## Run Locally
```bash
npm run dev
```







